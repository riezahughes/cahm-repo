version: 0.2
phases:
  install:
    commands:
      - cd frontend
      - npm i
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - cd dist
      - zip -r artifact.zip .
artifacts:
  files:
    - frontend/dist/artifact.zip
  name: sitebuild
